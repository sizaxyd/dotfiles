pcm.usb
{
    type hw
    card U0x46d0x825
}

# HDMI output
#pcm.!default {
#	type hw
#	card 1
#  device 3
#}

pcm.!default
{
    type asym
    playback.pcm
    {
        type plug
        slave.pcm "dmix"
    }
    capture.pcm
    {
        type plug
        slave.pcm "usb"
    }
}